# Server port
PORT=8787

# Allowed origin(s) for CORS + Socket.IO (comma separated). The Vite dev server default.
CORS_ORIGIN=http://localhost:5173

# Google Street View Static API key. Required for the road-side "quiz photo" feature.
# Kept server-side only now — it is never sent to the browser.
GOOGLE_STREETVIEW_KEY=
